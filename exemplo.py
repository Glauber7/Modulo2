def find_max(nums):
  max_num = float( "-inf" ) # menor que todos os outros números
    for num in nums:
   #     if num > max_num:
    #        max_num += 1
 # (Prnueencha a linha que falta aqui)
    #return max_num
